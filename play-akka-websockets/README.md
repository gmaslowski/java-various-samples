Play + Akka + WebSockets
========================
Sample application showing how easily Play integrates Akka and WebSockets.

## Usage
To try it, simply clone the project and run
```bash
sbt run
```

Then navigate to localhost:9000 to see results.
